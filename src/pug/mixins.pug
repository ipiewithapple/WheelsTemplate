- const navItems = ["about us", "products", "services", "our team", "blog", "contact"]

mixin nav
  span.logo wheels
  ul.nav__list
    each el in navItems
      li.nav__item: a.nav__link(href= "#" + el.replace(" ", "-"))= el
